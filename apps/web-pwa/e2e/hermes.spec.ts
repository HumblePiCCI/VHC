import { test } from '@playwright/test';

test.describe.skip('Hermes messaging and forum (E2E)', () => {
  test('Alice sends message to Bob', async () => {
    // Placeholder for Sprint 3 E2E; relies on VITE_E2E_MODE mocks.
  });

  test('trusted user creates thread and comment', async () => {
    // Placeholder for Sprint 3 E2E; relies on VITE_E2E_MODE mocks.
  });

  test('untrusted user sees gate', async () => {
    // Placeholder for Sprint 3 E2E; relies on VITE_E2E_MODE mocks.
  });
});
